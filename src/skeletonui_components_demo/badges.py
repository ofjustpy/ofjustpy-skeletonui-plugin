from skeletonui_components import *
from py_tailwind_utils import *
import ofjustpy as oj

oj.set_style("un")
badge_vf = oj.PC.Span(text="filled", extra_classes="badge text-2xl font-semibold w-64 variant-filled-primary")

badge_vf_primary = oj.PC.Span(text="filled/primary", extra_classes="badge text-2xl font-semibold w-64 variant-filled-primary")

badge_vf_secondary = oj.PC.Span(text="filled/secondary", extra_classes="badge text-2xl font-semibold w-64 variant-filled-secondary")

badge_vf_tertiary = oj.PC.Span(text="filled/tertiary", extra_classes="badge text-2xl font-semibold w-64 variant-filled-tertiary")

badge_vf_success = oj.PC.Span(text="filled/success", extra_classes="badge text-2xl font-semibold w-64 variant-filled-success")

badge_vf_warning = oj.PC.Span(text="filled/warning", extra_classes="badge text-2xl font-semibold w-64 variant-filled-warning")

badge_vf_error = oj.PC.Span(text="filled/error", extra_classes="badge text-2xl font-semibold w-64 variant-filled-error")

badge_vf_surface = oj.PC.Span(text="filled/surface", extra_classes="badge text-2xl font-semibold w-64 variant-filled-surface")



all_badges_vf = [badge_vf,
              badge_vf_primary,
              badge_vf_secondary,
              badge_vf_tertiary,
              badge_vf_success,
              badge_vf_warning,
              badge_vf_error,
              badge_vf_surface
              ]

badge_vg = oj.PC.Span(text="ghost", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-primary")
badge_vg_primary = oj.PC.Span(text="ghost/primary", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-primary")

badge_vg_secondary = oj.PC.Span(text="ghost/secondary", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-secondary")

badge_vg_tertiary = oj.PC.Span(text="ghost/tertiary", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-tertiary")

badge_vg_success = oj.PC.Span(text="ghost/success", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-success")

badge_vg_warning = oj.PC.Span(text="ghost/warning", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-warning")

badge_vg_error = oj.PC.Span(text="ghost/error", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-error")

badge_vg_surface = oj.PC.Span(text="ghost/surface", extra_classes="badge text-2xl font-semibold w-64 variant-ghost-surface")

all_badges_vg = [badge_vg,
              badge_vg_primary,
              badge_vg_secondary,
              badge_vg_tertiary,
              badge_vg_success,
              badge_vg_warning,
              badge_vg_error,
              badge_vg_surface
              ]

badge_vs = oj.PC.Span(text="soft", extra_classes="badge text-2xl font-semibold w-64 variant-soft")

badge_vs_primary = oj.PC.Span(text="soft/primary", extra_classes="badge text-2xl font-semibold w-64 variant-soft-primary")

badge_vs_secondary = oj.PC.Span(text="soft/secondary", extra_classes="badge text-2xl font-semibold w-64 variant-soft-secondary")

badge_vs_tertiary = oj.PC.Span(text="soft/tertiary", extra_classes="badge text-2xl font-semibold w-64 variant-soft-tertiary")

badge_vs_success = oj.PC.Span(text="soft/success", extra_classes="badge text-2xl font-semibold w-64 variant-soft-success")

badge_vs_warning = oj.PC.Span(text="soft/warning", extra_classes="badge text-2xl font-semibold w-64 variant-soft-warning")

badge_vs_error = oj.PC.Span(text="soft/error", extra_classes="badge text-2xl font-semibold w-64 variant-soft-error")

badge_vs_surface = oj.PC.Span(text="soft/surface", extra_classes="badge text-2xl font-semibold w-64 variant-soft-surface")


all_badges_vs = [badge_vs,
              badge_vs_primary,
              badge_vs_secondary,
              badge_vs_tertiary,
              badge_vs_success,
              badge_vs_warning,
              badge_vs_error,
              badge_vs_surface
              ]

badge_vr = oj.PC.Span(text="ringed", extra_classes="badge text-2xl font-semibold w-64 variant-ringed")

badge_vr_primary = oj.PC.Span(text="ringed/primary", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-primary")

badge_vr_secondary = oj.PC.Span(text="ringed/secondary", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-secondary")

badge_vr_tertiary = oj.PC.Span(text="ringed/tertiary", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-tertiary")

badge_vr_success = oj.PC.Span(text="ringed/success", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-success")

badge_vr_warning = oj.PC.Span(text="ringed/warning", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-warning")

badge_vr_error = oj.PC.Span(text="ringed/error", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-error")

badge_vr_surface = oj.PC.Span(text="ringed/surface", extra_classes="badge text-2xl font-semibold w-64 variant-ringed-surface")


all_badges_vr = [badge_vr,
              badge_vr_primary,
              badge_vr_secondary,
              badge_vr_tertiary,
              badge_vr_success,
              badge_vr_warning,
              badge_vr_error,
              badge_vr_surface
              ]

badge_vglass = oj.PC.Span(text="glass", extra_classes="badge text-2xl font-semibold w-64 variant-glass")

badge_vglass_primary = oj.PC.Span(text="glass/primary", extra_classes="badge text-2xl font-semibold w-64 variant-glass-primary")

badge_vglass_secondary = oj.PC.Span(text="glass/secondary", extra_classes="badge text-2xl font-semibold w-64 variant-glass-secondary")

badge_vglass_tertiary = oj.PC.Span(text="glass/tertiary", extra_classes="badge text-2xl font-semibold w-64 variant-glass-tertiary")

badge_vglass_success = oj.PC.Span(text="glass/success", extra_classes="badge text-2xl font-semibold w-64 variant-glass-success")

badge_vglass_warning = oj.PC.Span(text="glass/warning", extra_classes="badge text-2xl font-semibold w-64 variant-glass-warning")

badge_vglass_error = oj.PC.Span(text="glass/error", extra_classes="badge text-2xl font-semibold w-64 variant-glass-error")

badge_vglass_surface = oj.PC.Span(text="glass/surface", extra_classes="badge text-2xl font-semibold w-64 variant-glass-surface")

all_badges_vglass = [badge_vglass,
              badge_vglass_primary,
              badge_vglass_secondary,
              badge_vglass_tertiary,
              badge_vglass_success,
              badge_vglass_warning,
              badge_vglass_error,
              badge_vglass_surface
              ]

all_badges = [*all_badges_vf,
              *all_badges_vg,
              *all_badges_vs,
              *all_badges_vr,
              *all_badges_vglass,
    ]

badges_box = oj.HCCStatic.StackW(key="Badgesbox",
                                 childs = all_badges,
                                 extra_classes="variant-ghost-tertiary w-full card p-4 space-y-4 space-x-4 items-center"
                                 )
